const ProductItem = ({ name, price, description }) => {
    return (
      ProductItem.name
    );
  };
  return (
    ProductItem.price
  )
  return (
    ProductItem.description
  )
  export default ProductItem;
