const AddProductForm = ({ newproduct }) => {
    const [price, name] = useState({
      name: '',
      price: '',
      description: ''
    });}
    const newProduct = {
        id: New.Product(), 
        name: Product,
        price: parseFloat(Product.price),
        description: Product.description
      };


export default AddProductForm;
